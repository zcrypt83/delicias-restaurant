# Ejemplo de variables de entorno para el backend (usando SQLite)
# SQLite no requiere servidor, la BD se guarda en un archivo local

# Ruta a la base de datos SQLite (relativa a backend/)
# Si no especificas, por defecto usa: ./data/delicias.db
DB_PATH=./data/delicias.db

# Puerto del servidor
PORT=5000

# JWT Secret para tokens de autenticación
JWT_SECRET=your_jwt_secret_key_change_in_production

# Para producción con MySQL, descomenta y actualiza:
# DB_HOST=production-server.com
# DB_PORT=3306
# DB_USER=prod_user
# DB_PASSWORD=secure_password
# DB_NAME=delicias_prod

